# DDF00063 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00063
check_id: CHK0153
severity: WARNING
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: AliasCode
entity: AliasCode
attributes: standardCodeAliases
text: A standard code alias is not expected to be equal to the standard code (e.g. no equal code or decode for the same coding system version is expected).
message: The standard code alias is the same as the standard code - the same combination of code system, code system version and either code or decode is used for both the standard code and the standard
  code alias (case insensitive).
_core_conditions_reference:
  all:
  - name: get_dataset
    operator: equal_to
    value:
      comparator: AliasCode
      target: instanceType
  - name: get_dataset
    operator: equal_to
    value:
      comparator: standardCodeAliases
      target: parent_rel.Code
      value_is_literal: true
  - name: get_dataset
    operator: equal_to_case_insensitive
    value:
      comparator: codeSystem
      target: standardCode.codeSystem
  - name: get_dataset
    operator: equal_to_case_insensitive
    value:
      comparator: codeSystemVersion
      target: standardCode.codeSystemVersion
  - any:
    - name: get_dataset
      operator: equal_to_case_insensitive
      value:
        comparator: code
        target: standardCode.code
    - name: get_dataset
      operator: equal_to_case_insensitive
      value:
        comparator: decode
        target: standardCode.decode
