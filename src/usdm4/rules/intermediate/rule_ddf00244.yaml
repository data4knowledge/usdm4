# DDF00244 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00244
check_id: CHK0303
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: NarrativeContentItem
entity: NarrativeContentItem
attributes: text
text: Referenced items in the narrative content item texts must be available elsewhere in the data model.
message: The item referenced in the narrative content item text is not available elsewhere in the model.
_core_jsonata_reference: "(\n  $lkp:=**[id and instanceType].$each(function($v,$k){{$join([instanceType,id,$k],\"|\"):$v}})~>$merge;\n  ($.study.versions.narrativeContentItems[$type(text)=\"string\" and\
  \ $contains(text,/usdm:ref/)])@$nci.\n    $match( $nci.text,\n            /<usdm:ref([^>]*)(\\/>|><\\/usdm:ref>)/\n    )@$ref.\n    (\n      $g0_or_null := function($m){$m ? $m.groups[0] : \"\"};\n  \
  \    {\n        \"instanceType\": $nci.instanceType,\n        \"id\": $nci.id,\n        \"path\": $nci._path,                     \n        \"name\": $nci.name,\n        \"text\": $nci.text,\n       \
  \ \"usdm_ref\": {\n                      \"match\": $ref.match,\n                      \"klass\": $match($ref.groups[0],/klass=\\\"([a-zA-Z]+)\\\"/) ~> $g0_or_null(),\n                      \"id\": $match($ref.groups[0],/id=\\\
  \"(\\w+)\\\"/) ~> $g0_or_null(),\n                      \"attribute\": $match($ref.groups[0],/attribute=\\\"([a-zA-Z]+)\\\"/) ~> $g0_or_null()\n                    }\n      }\n    )\n    ~>  $map(function($v)\n\
  \          {\n            (\n              $ref_val := \"usdm_ref\" in $keys($v)\n                          ? (\n                              $k := $join([$v.usdm_ref.klass,$v.usdm_ref.id,$v.usdm_ref.attribute],\"\
  |\");\n                              $k in $keys($lkp)\n                              ? $lookup($lkp,$k)\n                              : \"!!NOT FOUND!!\"\n                            )\n           \
  \               : $v.value;\n              $v ~> |$|{\"Invalid Reference\": $v.usdm_ref.match, \"Referenced Value\": $ref_val},['usdm_ref']|\n            )\n          }\n        )\n    ~>  $filter(function($v)\n\
  \          {\n            $v.`Referenced Value` = \"!!NOT FOUND!!\"\n          }\n        )\n)"
