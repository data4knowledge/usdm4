# DDF00069 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00069
check_id: CHK0091
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: StudyCell
entity: StudyCell
attributes: arm, epoch
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Each combination of arm and epoch must occur no more than once within a study design.
message: The combination of arm and epoch occurs more than once within the study design.
_core_jsonata_reference: "$.study.versions.studyDesigns@$sd.\n  $filter($sd.studyCells,function($v,$i,$a)\n    {\n      $count($a[armId=$v.armId and epochId=$v.epochId])>1\n    }\n  )@$d.\n    ($sd.arms[id=$d.armId])@$sa.\n\
  \      ($sd.epochs[id=$d.epochId])@$se.\n        {\n          \"instanceType\": $d.instanceType,\n          \"id\": $d.id,\n          \"path\": $d._path,\n          \"StudyDesign.id\": $sd.id,\n     \
  \     \"StudyDesign.name\": $sd.name,\n          \"armId\": $d.armId,\n          \"StudyArm.name\": $sa.name,\n          \"epochId\": $d.epochId,\n          \"StudyEpoch.name\": $se.name\n        } ~>\n\
  \        $sort(function($l,$r)\n            {\n              $join([$l.`StudyDesign.id`,$l.armId,$l.epochId],\"/\") >\n              $join([$r.`StudyDesign.id`,$r.armId,$r.epochId],\"/\")\n          \
  \  }\n        )"
