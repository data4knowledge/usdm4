# DDF00101 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00101
check_id: CHK0099
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: conditional
class: Procedure
entity: Procedure
attributes: studyIntervention
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Within a study design, if study type is Interventional then at least one intervention is expected to be referenced from a procedure.
message: The study type is Interventional (code=C98388) but a study intervention is not referenced from any procedure.
_core_jsonata_reference: "($.study.versions[$not(\"studyType\" in $keys($)) or studyType.code = \"C98388\"])@$sv.\n  ($sv.studyDesigns[$not(\"studyType\" in $keys($)) or studyType.code = \"C98388\"])@$sd.\n\
  \    {\n      \"instanceType\": $sd.instanceType,\n      \"id\": $sd.id,\n      \"path\": $sd._path,\n      \"name\": $sd.name,\n      \"studyType.code\": $append($sv.studyType.code,$sd.studyType.code),\n\
  \      \"studyType.decode\": $append($sv.studyType.decode,$sd.studyType.decode),\n      \"# Referenced Study Interventions\":\n          $count($sd.activities.definedProcedures\n            [\n      \
  \        studyInterventionId in\n                $append($sd.studyInterventionIds[$ in $sv.studyInterventions.id],$sd.studyInterventions.id)\n            ]\n          )\n    }[`# Referenced Study Interventions`\
  \ < 1][]"
