# DDF00051 — HIGH_CT_MEMBER — auto-generated by tools/generate_rule_intermediate.py
# Source: core-structured
id: DDF00051
check_id: CHK0109
severity: ERROR
classification: HIGH_CT_MEMBER
confidence: high
source: core-structured
review_required: false
predicate: ct-member
class: Timing
entity: Timing
attribute: type
attributes: type
codelist_ref: C201264
text: A timing's type must be specified using the Timing Type Value Set Terminology (C201264) DDF codelist.
message: The timing's type is not specified using the Timing Type Value Set Terminology (C201264) DDF codelist - codeSystem is not "http://www.cdisc.org", codeSystemVersion is not a valid terminology package
  date, the code or decode (either as preferred term or as submission value) is found in the codelist (case insensitive) but the corresponding decode or code does not match the codelist value (case sensitive),
  and/or neither code nor decode is found in the codelist.
_core_conditions_reference:
  all:
  - name: get_dataset
    operator: equal_to
    value:
      comparator: definition
      target: rel_type
  - name: get_dataset
    operator: equal_to
    value:
      comparator: true
      target: type
  - not:
      all:
      - name: get_dataset
        operator: equal_to
        value:
          comparator: http://www.cdisc.org
          target: type.codeSystem
      - name: get_dataset
        operator: is_contained_by
        value:
          comparator: $valid_versions
          target: type.codeSystemVersion
      - any:
        - all:
          - name: get_dataset
            operator: non_empty
            value:
              comparator: null
              target: $pref_term_for_code
          - name: get_dataset
            operator: non_empty
            value:
              comparator: null
              target: $value_for_code
          - any:
            - name: get_dataset
              operator: non_empty
              value:
                comparator: null
                target: $code_for_decode_pref_term
            - name: get_dataset
              operator: non_empty
              value:
                comparator: null
                target: $code_for_decode_value
          - any:
            - name: get_dataset
              operator: equal_to
              value:
                comparator: $code_for_decode_pref_term
                target: type.code
            - name: get_dataset
              operator: equal_to
              value:
                comparator: $code_for_decode_value
                target: type.code
          - any:
            - name: get_dataset
              operator: equal_to
              value:
                comparator: $pref_term_for_code
                target: type.decode
            - name: get_dataset
              operator: equal_to
              value:
                comparator: $value_for_code
                target: type.decode
        - all:
          - name: get_dataset
            operator: equal_to
            value:
              comparator: true
              target: $codelist_extensible
          - name: get_dataset
            operator: empty
            value:
              comparator: null
              target: $code_for_decode_pref_term
          - name: get_dataset
            operator: empty
            value:
              comparator: null
              target: $code_for_decode_value
          - name: get_dataset
            operator: empty
            value:
              comparator: null
              target: $pref_term_for_code
          - name: get_dataset
            operator: empty
            value:
              comparator: null
              target: $value_for_code
