# DDF00010 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00010
check_id: CHK0009
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: unique-within-scope
class: All
entity: All
attributes: name
text: The names of all child instances of the same parent class must be unique.
message: The same name has been used for more than one instance of the class.
_core_jsonata_reference: "**.*[id and instanceType and name].$\n    {\n        instanceType: {\n            name: [$.{\"id\": id, \"path\": _path, \"name\": name}]\n        } ~> $sift(function($v,$k){$count($v)>1})\n\
  \    }\n    ~> $each(function($v,$k){\n        $v.*.{\n            \"instanceType\":$k,\n            \"id\": id,\n            \"path\": path,\n            \"name\": name\n        }})\n    ~> $reduce($append)"
