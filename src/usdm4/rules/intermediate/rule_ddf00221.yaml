# DDF00221 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00221
check_id: CHK0265
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: InterventionalStudyDesign
entity: InterventionalStudyDesign, ObservationalStudyDesign
attributes: therapeuticAreas
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Within a study design, if more therapeutic areas are defined, they must be distinct.
message: The therapeutic areas of the study design are not unique - the same code is used more than once within the same codeSystem and codeSystemVersion.
_core_jsonata_reference: "study.versions.studyDesigns@$sd.(\n  $sd.therapeuticAreas.\n    {\n      \"group\": $join([codeSystem,codeSystemVersion,code],\"\\n\"),\n      \"details\": $\n    } {\n       \
  \ group:  $count(details)>1\n                ? {\n                    \"codeSystem\":$distinct(details.codeSystem),\n                    \"codeSystemVersion\":$distinct(details.codeSystemVersion),\n \
  \                   \"therapeuticAreas\":\"[\"&$join($.(details.id&\": \"&details.decode&\" (\"&details.code&\")\"),\"; \")&\"]\"\n                  }\n      }\n  ~>$each(function($v)\n      {\n     \
  \   {\n          \"instanceType\": $sd.instanceType,\n          \"id\": $sd.id,\n          \"path\": $sd._path,\n          \"name\": $sd.name,\n          \"therapeuticAreas.codeSystem\": $v.codeSystem,\n\
  \          \"therapeuticAreas.codeSystemVersion\": $v.codeSystemVersion,\n          \"therapeuticAreas\": $v.therapeuticAreas\n        }\n      }\n    )\n  )"
