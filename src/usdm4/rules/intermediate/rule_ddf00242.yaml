# DDF00242 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00242
check_id: CHK0286
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: required-attribute
class: Range
entity: Range
attributes: minValue, maxValue
text: For each range, a unit must be specified either for both the minimum and the maximum value, or for neither of them.
message: The unit is specified either for the minimum or the maximum value but not for both.
_core_jsonata_reference: "(study.**[instanceType=\"Range\"])@$rg.\n  $rg.\n      [($ValU:=function($v){$v.value&($v.unit? \" \" & $v.unit.standardCode.decode & \" (\"&$v.unit.standardCode.code&\")\": \"\
  \ (unit not specified)\")};\n          {\n              \"instanceType\": instanceType,\n              \"id\": id,\n              \"path\": _path,\n              \"minValue\": $ValU(minValue) ,\n    \
  \          \"maxValue\": $ValU(maxValue) ,\n              \"check\": $exists(minValue.unit.id) != $exists(maxValue.unit.id)\n          }\n      )][check=true]"
