# DDF00046 — HIGH_CT_MEMBER — auto-generated by tools/generate_rule_intermediate.py
# Source: core-structured
id: DDF00046
check_id: CHK0003
severity: ERROR
classification: HIGH_CT_MEMBER
confidence: high
source: core-structured
review_required: false
predicate: ct-member
class: ScheduledDecisionInstance
entity: Timing
attribute: parent_id
attributes: relativeFromScheduledInstance, relativeToScheduledInstance
scope:
  type: parent-of-class
  klasses:
  - ScheduleTimeline
text: A timing must only be specified as being relative to/from a scheduled activity/decision instance that is defined within the same timeline as the timing.
message: 'The relative to/from scheduled instance of a timing was not defined within the same timeline as the timing: the parent_id of the scheduled instance ($parent_of_scheduled_instance) is not the same
  as parent_id of the timing (parent_id.Timing).'
_core_conditions_reference:
  all:
  - any:
    - name: get_dataset
      operator: equal_to
      value:
        comparator: ScheduledActivityInstance
        target: instanceType
    - name: get_dataset
      operator: equal_to
      value:
        comparator: ScheduledDecisionInstance
        target: instanceType
  - name: get_dataset
    operator: equal_to
    value:
      comparator: reference
      target: rel_type
  - any:
    - name: get_dataset
      operator: equal_to
      value:
        comparator: relativeToScheduledInstanceId
        target: parent_rel
        value_is_literal: true
    - name: get_dataset
      operator: equal_to
      value:
        comparator: relativeFromScheduledInstanceId
        target: parent_rel
        value_is_literal: true
  - name: get_dataset
    operator: equal_to
    value:
      comparator: definition
      target: rel_type.Timing
  - name: get_dataset
    operator: is_not_contained_by
    value:
      comparator: $parent_of_scheduled_instance
      target: parent_id.Timing
