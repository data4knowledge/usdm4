# MANUAL: do not regenerate
# Predicate side specs hand-completed for stage-2 renderer.
id: DDF00261
check_id: CHK0316
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: false
predicate: biconditional
class: GeographicScope
entity: GeographicScope
attributes: code
text: If a geographic scope type is global then no code is expected to specify the specific area within scope while if it is not global then a code is expected to specify the specific area within scope.
message: The geographic scope is global (type.code = C68846) and a code has been specified for the specific area or the geographic scope is not global (type.code is not C68846) and no code has been specified
  for the specific area.
_core_jsonata_reference: "**[instanceType=\"GeographicScope\" and (type.code=\"C68846\")=($exists(code) and code)].\n  {\n    \"instanceType\": instanceType,\n    \"id\": id,\n    \"path\": _path,\n   \
  \ \"type.code\": type.code,\n    \"type.decode\": type.decode,\n    \"code.standardCode.code\": code.standardCode.code,\n    \"code.standardCode.decode\": code.standardCode.decode\n  }"
side_a:
  attr: type
  check: code_equals
  value: C68846
side_b:
  attr: code
  check: empty
