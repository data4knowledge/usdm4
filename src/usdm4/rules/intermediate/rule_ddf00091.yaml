# DDF00091 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00091
check_id: CHK0072
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: conditional
class: Condition
entity: Condition
attributes: appliesTo
text: When a condition applies to a procedure, activity, biomedical concept, biomedical concept category, or biomedical concept surrogate then an instance must be available in the corresponding class with
  the specified id.
message: The id value specified to indicate what the condition applies to does not reference a procedure, activity, biomedical concept, biomedical concept category, or biomedical concept surrogate - either
  the specified id is not an id for anything (it is invalid), or the instance identified by the id is not one of the expected classes.
_core_jsonata_reference: "(\n  $idityp:=$.study.versions.**[id]{id:instanceType};\n  $.study.versions.**.conditions.appliesToIds.\n    {\n      \"instanceType\": %.instanceType,\n      \"id\": %.id,\n \
  \     \"path\": %._path,\n      \"name\": %.name,\n      \"appliesTo id\": $,\n      \"appliesTo instanceType\": $ in $keys($idityp) ? $lookup($idityp,$) : \"[Invalid id]\"\n    }\n    [\n      $not(`appliesTo\
  \ instanceType` in\n        [\n          'Procedure',\n          'Activity',\n          'BiomedicalConcept',\n          'BiomedicalConceptCategory',\n          'BiomedicalConceptSurrogate'\n        ]\n\
  \      )\n    ]\n)"
