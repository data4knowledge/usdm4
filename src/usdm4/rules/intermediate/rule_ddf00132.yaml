# DDF00132 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00132
check_id: CHK0156
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: required-attribute
class: StudyDesignPopulation
entity: StudyDesignPopulation, StudyCohort
attributes: plannedCompletionNumber
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Within a study design, if a planned completion number is defined, it must be specified either in the study population or in all cohorts.
message: A planned completion number has been specified for both the study population and one or more cohorts, or it has been specified for only a subset of the cohorts.
_core_jsonata_reference: "($.**.studyDesigns)@$sd.\n  $sd.population@$p.\n  $p.\n      [\n          (\n              $NullCohort:=(\"null\" in cohorts.$type(plannedCompletionNumber) or ($count(cohorts.plannedCompletionNumber)!=$count(cohorts.id)));\
  \ \n              $InCohort:=$boolean(cohorts.plannedCompletionNumber);      \n              $InPop:=($type(plannedCompletionNumber) != \"null\" and $exists(plannedCompletionNumber));  \n            \
  \  $FValU:=function($n)\n                  {\n                      (\n                          $exists($n)\n                          ?   $type($n)=\"object\"\n                              ?   $exists($n.value)\n\
  \                                 ? $n.value&$n.unit.standardCode.decode\n                                 : $n.minValue.value&$n.minValue.unit.standardCode.decode&\" to \"&\n                        \
  \           $n.maxValue.value&$n.maxValue.unit.standardCode.decode\n                              : $string($n)\n                          : \"Missing\"\n                      )              \n      \
  \            };\n                  {\n                      \"instanceType\": $p.instanceType,\n                      \"id\": $p.id,\n                      \"path\": $p._path,\n                      \"\
  StudyDesign.id\": $sd.id,\n                      \"StudyDesign.name\": $sd.name,\n                      \"name\": $p.name,\n                      \"plannedCompletionNumber.id\": $p.plannedCompletionNumber.id,\n\
  \                      \"plannedCompletionNumber(value/range)\": $FValU($p.plannedCompletionNumber),\n                      \"cohorts.name\": \"[\"& $join($p.cohorts.(id & \": \" & name),\", \") & \"\
  ]\",\n                      \"cohorts.plannedCompletionNumber.id\": \"[\"& $join($p.cohorts.(id & \": \" & plannedCompletionNumber.id),\", \") & \"]\",\n                      \"cohorts.plannedCompletionNumber(value/range)\"\
  : \"[\"& $join($p.cohorts.(id & \": \" & $FValU(plannedCompletionNumber)),\", \") & \"]\",\n                      \"check\": (($InPop=true and $InCohort=true) or ($InPop=false and $NullCohort=true and\
  \ $InCohort=true))\n                      }\n          )\n      ]\n      [check=true]"
