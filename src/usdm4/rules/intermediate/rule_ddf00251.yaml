# DDF00251 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00251
check_id: CHK0295
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: id-reference-resolves
class: StudyCohort
entity: StudyCohort
attributes: indications
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: A study cohort must only reference indications that are  defined within the same study design.
message: The reference to the indication is not targetting an indication that has been defined within the same study design.
_core_jsonata_reference: "($.**.studyDesigns)@$sd.\n     $sd.population@$sp.\n     $sp.cohorts[$count(indicationIds[$not($ in $sd.indications.id)])>0].\n         {\n         \"instanceType\": instanceType,\n\
  \         \"id\": id,\n         \"path\": _path,\n         \"StudyDesign.id\": $sd.id,\n         \"StudyDesign.name\": $sd.name,\n         \"StudyDesign.indications.id\": $sd.indications.id,\n       \
  \  \"StudyDesignPopulation.id\": $sp.id,\n         \"StudyDesignPopulation.name\": $sp.name,\n         \"name\": name,\n         \"Invalid indicationIds\": indicationIds[$not($ in $sd.indications.id)]\n\
  \         }"
