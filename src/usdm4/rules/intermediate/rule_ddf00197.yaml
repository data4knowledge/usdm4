# DDF00197 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00197
check_id: CHK0241
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: unique-within-scope
class: ObservationalStudyDesign
entity: ObservationalStudyDesign, InterventionalStudyDesign
attributes: documentVersions
text: A study definition document version must not be referenced more than once by the same study design.
message: The study design references the same study definition document version more than once (there are duplicate id values in documentVersionIds).
_core_jsonata_reference: "study.versions.studyDesigns.\n  {\n    \"instanceType\": instanceType,\n    \"id\": id,\n    \"path\": _path,\n    \"name\": name,\n    \"Duplicate documentVersionIds\":\n    \
  \  $filter(documentVersionIds,function($v,$i,$a){$count($a[$=$v])>1}) ~> $sort\n  }[`Duplicate documentVersionIds`]"
