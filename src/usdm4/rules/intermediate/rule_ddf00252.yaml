# DDF00252 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00252
check_id: CHK0296
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: id-reference-resolves
class: StudyElement
entity: StudyElement
attributes: studyInterventions
text: A study element must only reference study interventions that are referenced by the same study design as the study element.
message: The reference to the intervention is not targetting an intervention that has been referenced by the study design as a study intervention.
_core_jsonata_reference: "($.**.versions)@$sv.\n  $sv.studyDesigns@$sd.\n          $sd.elements[$count(studyInterventionIds[$not ($ in $sd.studyInterventionIds)])>0].\n                          {\n    \
  \                      \"instanceType\": instanceType,\n                          \"id\": id,\n                          \"path\": _path,\n                          \"StudyDesign.id\": $sd.id,\n     \
  \                     \"StudyDesign.name\": $sd.name,\n                          \"StudyDesign.studyInterventionIds\": $sd.studyInterventionIds,\n                          \"name\": name,\n          \
  \                \"Invalid studyInterventionIds\": studyInterventionIds[$not ($ in $sd.studyInterventionIds)],\n                          \"Invalid StudyIntervention.name\": \"[\"&$join(studyInterventionIds[$not\
  \ ($ in $sd.studyInterventionIds)].($oid:=$;$oid&\": \"&($o:=$sv.studyInterventions[id=$oid];$o?$o.name:\"Not defined\")),\"; \")&\"]\"\n                          }"
