# DDF00167 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00167
check_id: CHK0187
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: unique-within-scope
class: StudyVersion
entity: StudyVersion
attributes: documentVersions
text: A study definition document version must not be referenced more than once by the same study version.
message: The study version references the same study definition document version more than once (there are duplicate id values in documentVersionIds).
_core_jsonata_reference: "study.versions.\n  {\n    \"instanceType\": instanceType,\n    \"id\": id,\n    \"path\": _path,\n    \"versionIdentifier\": versionIdentifier,\n    \"Duplicate documentVersionIds\"\
  :\n      $filter(documentVersionIds,function($v,$i,$a){$count($a[$=$v])>1}) ~> $sort\n  }[`Duplicate documentVersionIds`]"
