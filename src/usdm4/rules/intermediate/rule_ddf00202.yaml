# DDF00202 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00202
check_id: CHK0246
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: StudyRole
entity: StudyRole
attributes: organizations
text: The sponsor study role must point to exactly one organization.
message: The study role is a sponsor role (code.code is C70793) but it does not point to exactly one organization - it does not have exactly one value for organizationIds that is a valid id value for an
  organization.
_core_jsonata_reference: "study.versions@$sv.\n  ($sv.roles[code.code = \"C70793\"])@$r.\n    $r[$not($count(organizationIds) = 1 and $count($sv.organizations[id in $r.organizationIds]) = 1)].\n      {\n\
  \        \"instanceType\": $r.instanceType,\n        \"id\": $r.id,\n        \"path\": $r._path,\n        \"name\": $r.name,\n        \"code\": $r.code.decode & \" (\" & $r.code.code & \")\",\n      \
  \  \"organizationIds\": $r.organizationIds\n          ? \"[\"&$join($r.organizationIds.($oid:=$;$oid&\": \"&($o:=$sv.organizations[id=$oid];$o?$o.name:\"Invalid organizationId\")),\"; \")&\"]\",\n   \
  \     \"# Valid Organizations\": $count($sv.organizations[id in $r.organizationIds])\n      }"
