# DDF00172 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00172
check_id: CHK0193
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: StudyIdentifier
entity: StudyIdentifier
attributes: scope
text: There must be exactly one sponsor study identifier (i.e., a study identifier whose scope is an organization that is identified as the organization for the sponsor study role).
message: The study version does not have exactly one sponsor study identifier.
_core_jsonata_reference: "[$.study.versions@$sv.\n  (\n    $spIds := $sv.studyIdentifiers[scopeId in $sv.roles[code.code=\"C70793\"].organizationIds];\n    {\n      \"instanceType\": $sv.instanceType,\n\
  \      \"id\": $sv.id,\n      \"path\": $sv._path,\n      \"versionIdentifier\": $sv.versionIdentifier,\n      \"# Sponsor Identifiers\": $count($spIds),\n      \"Sponsor Identifiers\": $spIds.\n    \
  \    (\n          $spid:=$;\n          $spid.id & \": \" & $spid.text & \" (\" & $spid.scopeId & \n            \": \" & $sv.organizations[id=$spid.scopeId].name & \")\"\n        )\n    }\n  )][`# Sponsor\
  \ Identifiers` != 1]"
