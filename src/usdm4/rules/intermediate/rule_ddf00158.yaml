# DDF00158 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00158
check_id: CHK0173
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: required-attribute
class: ObservationalStudyDesign
entity: ObservationalStudyDesign, InterventionalStudyDesign
attributes: criteria
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Each defined eligibility criterion must be used by at least one study population or cohort within the same study design.
message: The eligibility criterion is not referenced by the study design's population or any of its cohorts.
_core_jsonata_reference: "($.**.studyDesigns)@$s.\n  $s.eligibilityCriteria@$ec\n    [\n      $not($ec.id in $append($s.population.criterionIds,$s.population.cohorts.criterionIds))\n    ].\n    {\n    \
  \  \"instanceType\": $ec.instanceType,\n      \"id\": $ec.id,\n      \"path\": $ec._path,\n      \"StudyDesign.id\": $s.id,\n      \"StudyDesign.name\": $s.name,\n      \"name\": $ec.name,\n      \"category\"\
  : $ec.category.decode,\n      \"identifier\": $ec.identifier\n    }"
