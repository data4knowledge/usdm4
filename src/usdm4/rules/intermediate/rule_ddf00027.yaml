# DDF00027 — HIGH_UNIQUE_WITHIN_SCOPE — auto-generated by tools/generate_rule_intermediate.py
# Source: core-structured
id: DDF00027
check_id: CHK0043
severity: ERROR
classification: HIGH_UNIQUE_WITHIN_SCOPE
confidence: high
source: core-structured
review_required: false
predicate: unique-within-scope
class: Activity
entity: Activity, EligibilityCriterion, Encounter, NarrativeContent, StudyEpoch
attribute: nextId
attributes: previous, next
text: To ensure consistent ordering, the same instance must not be referenced more than once as previous or next.
message: The same instance is referenced more than once as previous or next - nextId or previousId values are not unique.
_core_conditions_reference:
  all:
  - name: get_dataset
    operator: equal_to
    value:
      comparator: definition
      target: rel_type
  - any:
    - all:
      - name: get_dataset
        operator: non_empty
        value:
          comparator: null
          target: nextId
      - name: get_dataset
        operator: is_not_unique_set
        value:
          comparator:
          - parent_entity
          - parent_id
          - parent_rel
          target: nextId
    - all:
      - name: get_dataset
        operator: non_empty
        value:
          comparator: null
          target: previousId
      - name: get_dataset
        operator: is_not_unique_set
        value:
          comparator:
          - parent_entity
          - parent_id
          - parent_rel
          target: previousId
