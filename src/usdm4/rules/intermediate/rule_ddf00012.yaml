# DDF00012 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00012
check_id: CHK0030
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: ScheduleTimeline
entity: ScheduleTimeline
attributes: mainTimeline
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Within a study design, there must be exactly one scheduled timeline which identifies as the main Timeline.
message: The study design does not have exactly one main timeline.
_core_jsonata_reference: "$.study.versions.studyDesigns.\n  {\n      \"instanceType\": instanceType,\n      \"id\": id,\n      \"path\": _path,\n      \"name\": name,\n      \"# Main timelines\": $count(scheduleTimelines[mainTimeline=true]),\n\
  \      \"Main timelines\": scheduleTimelines[mainTimeline=true]\n                        ~> $map(function($v){$v.id & ($v.name ? \" [\" & $v.name & \"]\")})\n  }[`# Main timelines` != 1][]"
