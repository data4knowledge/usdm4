# DDF00191 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00191
check_id: CHK0235
severity: WARNING
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: StudyRole
entity: StudyRole
attributes: masking
text: A masking is not expected to be defined for any study role in a study design with an open label blinding schema.
message: A masking is defined for the study role, but the role applies to a study design with an open label blinding schema.
_core_jsonata_reference: "$.study.versions@$sv.\n  [\n    ($sv.roles)@$r.($sv.studyDesigns)@$sd\n      [\n        (\n            $sv.id in $r.appliesToIds or\n            $sd.id in $r.appliesToIds\n   \
  \     ) and\n        $r.masking.isMasked and\n        $sd.blindingSchema.standardCode.code = \"C49659\"\n      ].{\n          \"instanceType\": $r.instanceType,\n          \"id\": $r.id,\n          \"\
  path\": $r._path,\n          \"name\": $r.name,\n          \"code\": $r.code.decode,\n          \"masking.text\": $r.masking.text,\n          \"masking.isMasked\": $r.masking.isMasked,\n          \"appliesToIds\"\
  : $r.appliesToIds,\n          \"StudyDesign.id\": $sd.id,\n          \"StudyDesign.name\": $sd.name,\n          \"StudyDesign.blindingSchema\": $sd.blindingSchema.standardCode.decode\n        }\n  ]"
