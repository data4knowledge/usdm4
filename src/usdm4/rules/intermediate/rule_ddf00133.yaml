# DDF00133 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00133
check_id: CHK0157
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: required-attribute
class: StudyDesignPopulation
entity: StudyDesignPopulation, StudyCohort
attributes: plannedEnrollmentNumber
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: Within a study design, if a planned enrollment number is defined, it must be specified either in the study population or in all cohorts.
message: A planned enrollment number has been specified for both the study population and the cohorts, or it has been specified for only a subset of the cohorts.
_core_jsonata_reference: "($.**.studyDesigns)@$sd.\n  $sd.population@$p.\n  $p.\n      [\n          (\n              $NullCohort:=(\"null\" in cohorts.$type(plannedenrollmentNumber) or ($count(cohorts.plannedEnrollmentNumber)!=$count(cohorts.id)));\
  \ \n              $InCohort:=$boolean(cohorts.plannedEnrollmentNumber);      \n              $InPop:=($type(plannedEnrollmentNumber) != \"null\" and $exists(plannedEnrollmentNumber));  \n            \
  \  $FValU:=function($n)\n                  {\n                      (\n                          $exists($n)\n                          ?   $type($n)=\"object\"\n                              ?   $exists($n.value)\n\
  \                                 ? $n.value&$n.unit.standardCode.decode\n                                 : $n.minValue.value&$n.minValue.unit.standardCode.decode&\" to \"&\n                        \
  \           $n.maxValue.value&$n.maxValue.unit.standardCode.decode\n                              : $string($n)\n                          : \"Missing\"\n                      )              \n      \
  \            };\n                  {\n                      \"instanceType\": $p.instanceType,\n                      \"id\": $p.id,\n                      \"path\": $p._path,\n                      \"\
  StudyDesign.id\": $sd.id,\n                      \"StudyDesign.name\": $sd.name,\n                      \"name\": $p.name,\n                      \"plannedEnrollmentNumber.id\": $p.plannedEnrollmentNumber.id,\n\
  \                      \"plannedEnrollmentNumber(value/range)\": $FValU($p.plannedEnrollmentNumber),\n                      \"cohorts.name\": \"[\"& $join($p.cohorts.(id & \": \" & name),\", \") & \"\
  ]\",\n                      \"cohorts.plannedEnrollmentNumber.id\": \"[\"& $join($p.cohorts.(id & \": \" & plannedEnrollmentNumber.id),\", \") & \"]\",\n                      \"cohorts.plannedEnrollmentNumber(value/range)\"\
  : \"[\"& $join($p.cohorts.(id & \": \" & $FValU(plannedEnrollmentNumber)),\", \") & \"]\",\n                      \"check\": (($InPop=true and $InCohort=true) or ($InPop=false and $NullCohort=true and\
  \ $InCohort=true))\n                      }\n          )\n      ]\n      [check=true]"
