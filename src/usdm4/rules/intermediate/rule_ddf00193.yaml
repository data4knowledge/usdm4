# DDF00193 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00193
check_id: CHK0237
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: required-attribute
class: StudyRole
entity: StudyRole
attributes: masking
text: A masking is expected to be defined for at least one study role in a study design with a blinding schema that is not open label or double blind.
message: The study design has a blinding schema that is not open label or double blind (code is not C49659 or C15228) but there is no applicable study role that is masked.
_core_jsonata_reference: "($.study.versions)@$sv.\n  ($sv.studyDesigns)@$sd\n    [ \n      $sd.blindingSchema and\n      $not($sd.blindingSchema.standardCode.code in [\"C49659\",\"C15228\"])\n    ].[\n\
  \        ( \n          $maskedRoles := $sv.roles@$r\n                            [\n                              $sv.id in $r.appliesToIds or\n                              $sd.id in $r.appliesToIds\n\
  \                            ].{\n                                \"id\": $r.id,\n                                \"code\": $r.code.decode,\n                                \"isMasked\": $r.masking.isMasked\n\
  \                              };\n          {\n            \"instanceType\": $sd.instanceType,\n            \"id\": $sd.id,\n            \"path\": $sd._path,\n            \"name\": $sd.name,\n      \
  \      \"blindingSchema.code\": $sd.blindingSchema.standardCode.code,\n            \"blindingSchema.decode\": $sd.blindingSchema.standardCode.decode,\n            \"# Masked Roles\": $count($maskedRoles[isMasked=true]),\n\
  \            \"Applicable Roles\":\n              $maskedRoles\n              ~>  $map(function($v)\n                    {$join([$v.code,'[',$v.id,',',$v.isMasked=true?'Masked':'Not Masked',']'])}\n \
  \                 )\n              ~> $join(\"; \")\n          }\n        )  \n      ][`# Masked Roles` < 1]"
