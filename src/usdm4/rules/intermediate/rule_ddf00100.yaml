# DDF00100 — HIGH_UNIQUE_WITHIN_SCOPE — auto-generated by tools/generate_rule_intermediate.py
# Source: core-structured
id: DDF00100
check_id: CHK0090
severity: ERROR
classification: HIGH_UNIQUE_WITHIN_SCOPE
confidence: high
source: core-structured
review_required: false
predicate: unique-within-scope
class: StudyTitle
entity: StudyVersion
attribute: type.code
attributes: titles
scope:
  type: parent-of-class
  klasses:
  - StudyVersion
text: Within a study version, there must be no more than one title of each type.
message: There is more than one title of the same type code or decode.
_core_conditions_reference:
  all:
  - name: get_dataset
    operator: equal_to
    value:
      comparator: StudyTitle
      target: instanceType
  - name: get_dataset
    operator: equal_to
    value:
      comparator: definition
      target: rel_type
  - any:
    - name: get_dataset
      operator: is_not_unique_set
      value:
        comparator:
        - parent_id
        target: type.code
    - name: get_dataset
      operator: is_not_unique_set
      value:
        comparator:
        - parent_id
        target: type.decode
