# DDF00212 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00212
check_id: CHK0256
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: conditional
class: ProductOrganizationRole
entity: ProductOrganizationRole
attributes: appliesTo
text: If 'appliesTo' is specified for a product organization role, then the product organization role must only apply to medical devices or administrable products.
message: At least one of the 'appliesTo' specifications does not apply to medical devices or administrable products.
_core_jsonata_reference: "(study.versions)@$sv.\n  $sv.productOrganizationRoles@$pr.\n      [($a:=$sv.administrableProducts.id;\n        $d:=$sv.medicalDevices.id;\n        $medDev:=function($i){$sv.medicalDevices[id=$i].(id?\
  \ id &\": \"&name)};\n        $admProd:=function($i){$sv.administrableProducts[id=$i].(id? id &\": \"&name)};\n               $pr.{\n                  \"instanceType\": instanceType,\n               \
  \   \"id\": id,\n                  \"path\": _path,\n                  \"name\": name,\n                  \"appliesToIds\": appliesToIds ? \"[\" & $join(appliesToIds,\", \")&\"]\",\n                 \
  \ \"appliesTo name\": \"[\"&$join($map(appliesToIds,function($b){$medDev($b)?$medDev($b):($admProd($b)?$admProd($b):$b & \": Invalid reference\")}),\", \")&\"]\",\n                  \"check\": false in\
  \ $map(appliesToIds,function($v){($v in $a) or ($v in $d)})\n              }\n      )][check=true]"
