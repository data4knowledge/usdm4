# DDF00241 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00241
check_id: CHK0285
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: conditional
class: Range
entity: Range
attributes: minValue, maxValue
text: If the unit is the same (or missing) for both the minimum and maximum value, then the minimum value must be less than the maximum value.
message: The maximum value is less or equal to the minimum value while the unit is the same (or missing) for both the minimum and maximum value.
_core_jsonata_reference: "(study.**[instanceType=\"Range\"])@$rg.\n  $rg.\n      [($ValU:=function($v){$v.value&($v.unit? \" \" & $v.unit.standardCode.decode & \" (\"&$v.unit.standardCode.code&\")\": \"\
  \ (unit not specified)\")};\n       $EqUnit:=(minValue.unit.standardCode.code=maxValue.unit.standardCode.code) or ((minValue.unit=null or $exists(minValue.unit)=false) and (maxValue.unit=null or $exists(maxValue.unit)=false));\n\
  \          \n          {\n              \"instanceType\": instanceType,\n              \"id\": id,\n              \"path\": _path,\n              \"minValue\": $ValU(minValue) ,\n              \"maxValue\"\
  : $ValU(maxValue) ,\n              \"check\": $EqUnit and (maxValue.value <= minValue.value)\n          }\n      )][check=true]"
