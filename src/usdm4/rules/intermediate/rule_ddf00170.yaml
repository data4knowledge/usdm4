# DDF00170 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00170
check_id: CHK0191
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: unique-within-scope
class: Abbreviation
entity: Abbreviation
attributes: abbreviatedText
scope:
  type: parent-of-class
  klasses:
  - StudyVersion
text: All abbreviations defined for a study version must be unique.
message: The abbreviation's abbreviated text is not unique within the study version.
_core_jsonata_reference: "study.versions@$sv.\n  (\n    $filter($sv.abbreviations,function($v,$i,$a)\n      {$count($a[$.abbreviatedText=$v.abbreviatedText])>1}\n    )\n    ~> $sort(function($l,$r){$l.abbreviatedText>$r.abbreviatedText})\n\
  \  ).\n  {\n    \"instanceType\": instanceType,\n    \"id\": id,\n    \"path\": _path,\n    \"StudyVersion.id\": $sv.id,\n    \"StudyVersion.versionIdentifier\": $sv.versionIdentifier,\n    \"abbreviatedText\"\
  : abbreviatedText,\n    \"expandedText\": expandedText\n  }"
