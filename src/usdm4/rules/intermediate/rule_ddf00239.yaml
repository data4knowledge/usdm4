# DDF00239 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00239
check_id: CHK0283
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: Strength
entity: Strength
attributes: numerator
text: If a strength numerator range is specified, both the minValue and maxValue must have a unit.
message: The strength numerator range is specified but the minValue and/or maxValue does not have a unit.
_core_jsonata_reference: "(study.versions)@$sv.\n  $sv.administrableProducts@$ap.\n  $ap.ingredients@$in.\n  $in.substance@$su.\n  $su.**.strengths@$st.\n  $st.[($ValU:=function($v){$v.value&($v.unit? \"\
  \ \" & $v.unit.standardCode.decode & \" (\"&$v.unit.standardCode.code&\")\": \" (unit not specified)\")};\n          {\n              \"instanceType\": instanceType,\n              \"id\": id,\n     \
  \         \"path\": _path,\n              \"AdministrableProduct.id\": $ap.id,\n              \"AdministrableProduct.name\": $ap.name,\n              \"Ingredient.id\": $in.id,\n              \"Substance.id\"\
  : $su.id,\n              \"Substance.name\": $su.name,\n              \"name\": name,\n              \"numerator.minValue\": $ValU(numerator.minValue) ,\n              \"numerator.maxValue\": $ValU(numerator.maxValue)\
  \ ,\n              \"check\": (numerator.minValue.value and ($not(numerator.minValue.unit) or $exists(numerator.minValue.unit)=false)) or (numerator.maxValue.value and ($not(numerator.maxValue.unit) or\
  \ $exists(numerator.maxValue.unit)=false))\n          }\n      )][check=true]"
