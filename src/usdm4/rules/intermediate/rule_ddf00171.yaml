# DDF00171 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00171
check_id: CHK0192
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: unique-within-scope
class: Abbreviation
entity: Abbreviation
attributes: expandedText
scope:
  type: parent-of-class
  klasses:
  - StudyVersion
text: The expanded text for all abbreviations defined for a study version are expected to be unique.
message: The abbreviation's expanded text is not unique within the study version (case insensitive).
_core_jsonata_reference: "study.versions@$sv.\n  (\n    $filter($sv.abbreviations,function($v,$i,$a)\n      {$count($a[$lowercase($.expandedText)=$lowercase($v.expandedText)])>1}\n    )\n    ~> $sort(function($l,$r){$lowercase($l.expandedText)>$lowercase($r.expandedText)})\n\
  \  ).\n  {\n    \"instanceType\": instanceType,\n    \"id\": id,\n    \"path\": _path,\n    \"StudyVersion.id\": $sv.id,\n    \"StudyVersion.versionIdentifier\": $sv.versionIdentifier,\n    \"abbreviatedText\"\
  : abbreviatedText,\n    \"expandedText\": expandedText\n  }"
