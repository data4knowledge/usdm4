# DDF00243 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00243
check_id: CHK0304
severity: WARNING
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: StudyCell
entity: StudyCell
attributes: arm, epoch
text: Each StudyArm is expected to have one StudyCell for each StudyEpoch.
message: The StudyArm does not have one StudyCell for each StudyEpoch.
_core_jsonata_reference: "study.versions.studyDesigns@$sd.\n  $sd.arms@$sa.{\n  $sa.id:\n      {\n      \"instanceType\": $sa.instanceType,\n      \"id\": $sa.id,\n      \"path\": $sa._path,\n      \"StudyDesign.id\"\
  : $sd.id,\n      \"StudyDesign.name\": $sd.name,\n      \"name\": $sa.name,\n      \"StudyDesign.epochs\": \"[\"&$join($sd.epochs.(id&\": \"&name),\"; \")&\"]\",\n      \"Arm's StudyCell Epoch Refs\"\
  : \"[\"&$join($sd.studyCells[armId=$sa.id].($sc:=$;id&\": \"&epochId&\" (\"&($sc.epochId in $sd.epochs.id?$sd.epochs[id=$sc.epochId].name:\"Invalid epochId\")&\")\"),\"; \")&\"]\",\n      \"Missing Epoch\
  \ Refs\": \"[\"&$join($sd.epochs[$not(id in $sd.studyCells[armId=$sa.id].epochId)].(id&\": \"&name),\"; \")&\"]\"\n      }\n  }.*[`Missing Epoch Refs`!=\"[]\"]"
