# DDF00050 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00050
check_id: CHK0190
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: id-reference-resolves
class: StudyArm
entity: StudyArm
attributes: populations
scope:
  type: parent-of-class
  klasses:
  - InterventionalStudyDesign
  - ObservationalStudyDesign
text: A study arm must only reference study populations or cohorts that are defined within the same study design as the study arm.
message: The study arm references a populationId that does not match the id of any study population or study cohort defined within the same study design as the study arm.
_core_jsonata_reference: "($.**.studyDesigns)@$s.\n  [\n    $s.arms.populationIds[$ != $s.population.id and $not($ in $s.population.cohorts.id)].\n      {\n        \"instanceType\": %.instanceType,\n  \
  \      \"id\": %.id,\n        \"path\": %._path,\n        \"StudyDesign.id\": $s.id,\n        \"StudyDesign.name\": $s.name,\n        \"StudyDesign.population.id\": $s.population.id,\n        \"StudyDesign.population.cohorts.id\"\
  : $s.population.cohorts.id,\n        \"name\": %.name,\n        \"populationId\": $\n      }\n  ]"
