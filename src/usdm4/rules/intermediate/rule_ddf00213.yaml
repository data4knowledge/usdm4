# DDF00213 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00213
check_id: CHK0257
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: implication
class: InterventionalStudyDesign
entity: InterventionalStudyDesign
attributes: model
text: If the intervention model indicates a single group design then only one intervention is expected. In all other cases more interventions are expected.
message: The number of study interventions referenced for the interventional study design is not consistent with intervention model specified for the study design - the model indicates a cross-over, parallel
  or factorial design (model.code is C82637, C82639, or C82638) and there are not references to multiple study interventions.
_core_jsonata_reference: "($.study.versions)@$sv.\n  ($sv.studyDesigns[studyType.code = \"C98388\"])@$sd.\n    {\n      \"instanceType\": $sd.instanceType,\n      \"id\": $sd.id,\n      \"path\": $sd._path,\n\
  \      \"name\": $sd.name,\n      \"studyType.code\": $sd.studyType.code,\n      \"studyType.decode\": $sd.studyType.decode,\n      \"model.code\": $sd.model.code,\n      \"model.decode\": $sd.model.decode,\n\
  \      \"# Referenced Study Interventions\":  $count($distinct($sd.studyInterventionIds[$ in $sv.studyInterventions.id]))\n    }\n    [\n      (`model.code` in [\"C82637\",\"C82639\",\"C82638\"] and `#\
  \ Referenced Study Interventions` <= 1)\n    ]"
