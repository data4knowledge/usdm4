# DDF00189 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00189
check_id: CHK0233
severity: ERROR
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: mutual-exclusion
class: StudyRole
entity: StudyRole
attributes: appliesTo
text: Every study role must apply to either a study version or at least one study design, but not both.
message: The study role does not only apply to the study version or at least one study design - it does not apply to anything, it applies to both the study version and at least one study design, or it applies
  to something that is not a study version or a study design.
_core_jsonata_reference: "study.versions@$sv.\n  ($sv.roles\n    [\n      $not(\n          appliesToIds and\n            (\n                ($count(appliesToIds)=1 and appliesToIds[0]=$sv.id) or\n     \
  \           $count(appliesToIds)=$count(appliesToIds[$ in $sv.studyDesigns.id])\n            )\n      )\n    ]\n  )@$r.\n    {\n      \"instanceType\": $r.instanceType,\n      \"id\": $r.id,\n      \"\
  path\": $r._path,\n      \"name\": $r.name,\n      \"code\": $r.code.decode,\n      \"appliesToIds\": $r.appliesToIds[],\n      \"StudyVersion.id\": $sv.id,\n      \"StudyVersion.studyDesigns.id\": $sv.studyDesigns.id\n\
  \    }"
