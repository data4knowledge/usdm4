# DDF00094 — MED_TEXT — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00094
check_id: CHK0078
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: true
predicate: conditional
class: GeographicScope
entity: StudyVersion
attributes: dateValues
scope:
  type: parent-of-class
  klasses:
  - StudyVersion
text: Within a study version, if a date of a specific type exists with a global geographic scope then no other dates are expected with the same type.
message: The study version has a type of governance date with a global geographic scope and either another date of the same type or additional geographic scope(s) defined for the date.
_core_conditions_reference:
  all:
  - name: get_dataset
    operator: equal_to
    value:
      comparator: GeographicScope
      target: instanceType
  - name: get_dataset
    operator: equal_to
    value:
      comparator: definition
      target: rel_type
  - any:
    - name: get_dataset
      operator: equal_to
      value:
        comparator: C68846
        target: type.code
    - name: get_dataset
      operator: equal_to_case_insensitive
      value:
        comparator: Global
        target: type.decode
  - any:
    - name: get_dataset
      operator: greater_than
      value:
        comparator: 1
        target: $num_geographic_scopes_for_this_date
    - name: get_dataset
      operator: greater_than
      value:
        comparator: 1
        target: $num_dates_of_this_type
