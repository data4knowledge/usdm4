# MANUAL: do not regenerate
# Biconditional side specs hand-completed for stage-2 renderer.
# Stage-1 will detect predicate=biconditional from 'vice versa' in rule
# text but leaves side_a / side_b for a human to fill in.
id: DDF00039
check_id: CHK0309
severity: WARNING
classification: MED_TEXT
confidence: medium
source: rule-text
review_required: false
predicate: biconditional
class: Duration
entity: Duration
attributes: quantity
text: If the duration will vary, a quantity is not expected for the duration and vice versa.
message: The duration quantity is specified while the duration is indicated to vary or vice versa.
_core_jsonata_reference: "($.**[instanceType=\"Duration\"])@$d.\n  $d.[(\n          $ValU:=function($v){$v.value&($v.unit? \" \" & $v.unit.standardCode.decode & \" (\"&$v.unit.standardCode.code&\")\")};\n\
  \          $FValU:=function($n)\n              {\n                  (\n                      $exists($n)\n                          ?   $type($n)=\"object\"\n                              ?   $exists($n.value)\n\
  \                                  ? $ValU($n)\n                                  : $exists($n.minValue) or $exists($n.maxValue)\n                                      ? $ValU($n.minValue)&\" to \"&$ValU($n.maxValue)\n\
  \                                      : $string($n)\n                              : $string($n)\n                          : \"Missing\"\n                  )              \n              };{\n     \
  \                     \"instanceType\": instanceType,\n                          \"id\": id,\n                          \"path\": _path,\n                          \"quantity(value/range)\": $FValU(quantity),\n\
  \                          \"durationWillVary\": durationWillVary,\n                          \"check\": (durationWillVary and quantity) or (durationWillVary=false and ($not(quantity) or $exists(quantity)=false))\n\
  \                          })][check=true]"
side_a:
  attr: durationWillVary
  check: equal_to_bool
  value: true
side_b:
  attr: quantity
  check: empty
