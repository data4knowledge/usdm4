# DDF00159 — LOW_CUSTOM — auto-generated by tools/generate_rule_intermediate.py
# Source: rule-text
id: DDF00159
check_id: CHK0174
severity: ERROR
classification: LOW_CUSTOM
confidence: low
source: rule-text
review_required: true
predicate: custom
class: ObservationalStudyDesign
entity: ObservationalStudyDesign, InterventionalStudyDesign
attributes: criteria
text: An eligibility criterion must not be referenced by both a study design population and any of the cohorts of the same study design population.
message: The eligibility criterion is referenced by both the study design's population and at least one of its cohorts.
_core_jsonata_reference: "($.**.studyDesigns)@$s.\n  $s.eligibilityCriteria@$ec\n    [\n      $ec.id in $s.population.criterionIds and\n      $ec.id in $s.population.cohorts.criterionIds\n    ].\n    {\n\
  \      \"instanceType\": $ec.instanceType,\n      \"id\": $ec.id,\n      \"path\": $ec._path,\n      \"StudyDesign.id\": $s.id,\n      \"StudyDesign.name\": $s.name,\n      \"name\": $ec.name,\n     \
  \ \"category\": $ec.category.decode,\n      \"identifier\": $ec.identifier,\n      \"Used in\": $s.**[$ec.id in $.criterionIds].(id&(name?\"[\"&name&\"]\"))\n    }"
